{% if product %}
  {% assign active_variant = product.selected_or_first_available_variant %}
  <span>
    {% if active_variant.compare_at_price > active_variant.price %}
      <s class="meta">{{ active_variant.compare_at_price | money }}</s>
    {% endif %}
    {{ active_variant.price | money }}
  </span>
{% endif %}
